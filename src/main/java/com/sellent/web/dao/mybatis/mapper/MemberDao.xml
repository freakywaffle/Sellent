<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sellent.web.dao.MemberDao">

<select id="select"  resultType="int">
	select count(id) from member where id= #{param1}
</select>

<select id="getMemberById" parameterType="java.lang.String" resultType="com.sellent.web.entity.Member">
	select * from member where id=#{param1}
</select>

<insert id="insert" parameterType="com.sellent.web.entity.Member" >
	insert into 
	member(
		id,
		password,
		nickname,
		photo,
		simple_introduction,
		detail_introduction,
		email
	) 
	values(
		#{id},
		#{password},
		#{nickname},
		#{photo},
		#{simple_introduction},
		#{detail_introduction},
		#{email}
	);
</insert>
</mapper>